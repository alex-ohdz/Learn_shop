

const Workers = () => {
  // Add state and effect hooks to handle workers
  return (
    <div>
      <h2>Workers</h2>
      {/* Add content and functionality for Workers */}
    </div>
  );
};

export default Workers;



// const Workers = () => {
//   // Add state and effect hooks to handle workers
//   return (
//     <div>
//       <h2>Workers</h2>
//       {/* Add content and functionality for Workers */}
//     </div>
//   );
// };

// export default Workers;
//  en los trabajadores vamos a tener name, rol e imagen , el diseno seria un botn para agregar nuevo trabajador levanta un modal en el que se selecciona la imagen, se escribe el rol y el nombre del trabajador luego se acepta y se envia a la base de datos a la tabla workers y se cargan desde ahi para mostrar al administrador los ya contenidos